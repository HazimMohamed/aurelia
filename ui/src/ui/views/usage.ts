// Stub — usage view not used in Aurelia
import { nothing } from "lit";
export type SessionLogEntry = Record<string, unknown>;
export type SessionLogRole = string;
export function renderUsage(_props: unknown) { return nothing; }
