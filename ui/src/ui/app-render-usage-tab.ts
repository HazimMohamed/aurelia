// Stub — usage tab not used in Aurelia
import { nothing } from "lit";
import type { AppViewState } from "./app-view-state.ts";

export function renderUsageTab(_state: AppViewState) {
  return nothing;
}
