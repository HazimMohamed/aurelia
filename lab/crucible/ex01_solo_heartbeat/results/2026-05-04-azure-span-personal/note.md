EX-01.2 saturation baseline: in faint-fracture (5 cycles, personal agent, blank karma) the agent wrote a new
  reflection every single cycle with no sign of slowing down. This run extends to 15 cycles with no other changes — same
  heartbeat prompt, same agent profile, no chained karma, no noop permission — to find where saturation occurs, if at all,
   and what it looks like when it does.